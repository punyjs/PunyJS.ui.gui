/**
* @test
*   @title PunyJS.ui.gui.template.handlers._Repeat: basic test
*/
function repeatTagHandlerTest(
    controller
) {

    arrange(
        function arrangeFn() {

        }
    );

    act(
        function actFn() {

        }
    );

    assert(
        function assertFn(test) {

        }
    );
}